addressable_lambda:
  name: "Christmas Tree - Static"
  lambda: |-
    for (int i = 0; i < it.size(); i++) {
      if (i % 2 == 1) {
        it[i] = Color(255, 0, 18, 0);
      } else {
        it[i] = Color(0, 179, 44, 0);
      }
    }